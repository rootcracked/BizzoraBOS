FUNCTION REGISTER(REGISTER REQUEST):
	VAR REGISTER REQUEST[AUTHMODEL_REQUEST]
  PARSE JSON REQUEST TO [REGISTER_REQUEST]
  IF FAILS OR JSON REQUEST DOES NOT MATCH [REGISTER_REQUEST]:
    RETURN 404 ERROR
  ELSE
    CALL AUTHSERVICE.REGISTER AND PASS PARSED REQUEST
    IF FAILS:
      SERVICE RETURN 404 ERROR
    ELSE:
      RETURN STATUS RESPONSE + [REGISTER_RESPONSE]

	
